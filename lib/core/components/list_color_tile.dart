import 'package:flutter/material.dart';

class ColorTileList extends StatelessWidget {
  const ColorTileList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
